using System;
using System.Collections.Generic;

namespace lv5
{
    class Program
    {
        static void Main(string[] args)
        {
            //zadatak 1 i 2
            //List<IShipable> list = new List<IShipable>();
            //Box box = new Box("dominik");
            //Product product = new Product("sds", 55.66, 4);
            //Product product1 = new Product("asdads", 55, 2);
            //list.Add(box);

            //list.Add(product);
            //list.Add(product1);

            //ShippingService price = new ShippingService(4);
            //double w = 0;
            //foreach (IShipable ship in list)
            //{

            //    w += ship.Weight;

            //    Console.WriteLine(ship.Description());

            //}

            //Console.WriteLine(price.ToString() + price.Price(w) + " kn");
            //zadatak 3
            // Dataset data1 = new Dataset("csv.txt");
            // User user1 = User.GenerateUser("Domink");
            // User user2 = User.GenerateUser("Skrinjar");

            // ProtectionProxyDataset proxy1 = new ProtectionProxyDataset(user1);
            // ProtectionProxyDataset proxy2 = new ProtectionProxyDataset(user2);

            // VirtualProxyDataset proxy3 = new VirtualProxyDataset("csv.txt");
            // DataConsolePrinter printer = new DataConsolePrinter();
            // printer.Print(proxy1);
            // Console.WriteLine();
            // printer.Print(proxy2);
            // Console.WriteLine();
            //Console.WriteLine("Virtual: \n");
            // printer.Print(proxy3);
            // zadatak 5 , 6 i 7
            //ITheme theme = new LightTheme();
            //ITheme theme1 = new DarkTheme();


            //ReminderNote note = new ReminderNote("Ja", theme);
            //ReminderNote note1 = new ReminderNote("Osijek", theme1);
            //GroupNote note3 = new GroupNote("Kohorta", theme);
            

            //Notebook notes = new Notebook(theme1);
            //notes.AddNote(note);
            //notes.Display();
            //notes.AddNote(note1);
            //notes.Display();
            //note3.insert("1988");
            //note3.Show();
           

        }
    }
}
